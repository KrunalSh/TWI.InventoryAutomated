@model  TWI.InventoryAutomated.Models.Company
@{
    Layout = null;
}

@using (Html.BeginForm("AddOrEdit", "Company", FormMethod.Post, new { onsubmit = "return SubmitForm(this)" }))
{
    @Html.HiddenFor(model => model.ID)
    <div id="txtNotification" style="visibility:hidden;height:30px;">Message</div>
    <div class="form-group" id="companyName">
        @Html.LabelFor(model => model.CompanyName, new { @class = "control-label" })
        @Html.EditorFor(model => model.CompanyName, new { htmlAttributes = new { @class = "form-control" } })
        @Html.ValidationMessageFor(model => model.CompanyName)
    </div>
    <div class="form-group">
        @Html.LabelFor(model => model.IsActive, new { @class = "control-label" })
        @{
            var IsActive = true;
            if (Model.IsActive != null)
            { IsActive = (bool)Model.IsActive; }
        }
        @Html.CheckBoxFor(x => IsActive)
    </div>


    <div class="form-group">
        <input type="submit" value="Save" class="btn btn-primary" />
        <input type="reset" value="Cancel" class="btn" onclick="Cancelbutton();"  />
    </div>
}